fn main() {
    let mut buf1 =String::new();    
    println!("Your age: ");      
    io::stdin().read_line(&mut x)
    .ok()
    .expect("error!");      
    let x_i32: Result<f64, _> = x1.trim().parse();

    let a = x.tan();
    let b = x.sin() / x.cos();

    assert_eq!(a, b);
}